apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: openbao
  namespace: openbao
spec:
  parentRefs:
    - name: main-gateway
      namespace: gateway-api-system
  hostnames:
    - openbao.production.k8s.evoteum.com
  rules:
    - backendRefs:
        - name: openbao-active
          port: 8200

apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: planzoco
  namespace: planzoco
spec:
  parentRefs:
    - name: cloudflare-gateway
      namespace: cloudflare-gateway
  hostnames:
    - planzo.co
  rules:
    - backendRefs:
        - name: planzoco
          port: 8080

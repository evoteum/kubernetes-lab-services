apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: ollama
  namespace: ollama
spec:
  parentRefs:
    - name: main-gateway
      namespace: gateway-api-system
  hostnames:
    - ollama.production.k8s.evoteum.com
  rules:
    - backendRefs:
        - name: ollama
          port: 30007
